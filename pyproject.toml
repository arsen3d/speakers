[project]
name = "speakers"
version = "0.1.0"
description = "Speaker diarization app using pyannote and gradio"
requires-python = ">=3.11,<3.13"
dependencies = [
    "gradio",
    "pyannote.audio",
    "torch",
    "huggingface_hub",
    "python-dotenv",
]
